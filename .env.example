# Database Configuration
# PostgreSQL connection string for your Supabase database
# Get from: Supabase Dashboard → Settings → Database → Connection Pooling (Transaction mode, port 6543)
DATABASE_URL="postgresql://user:password@host:6543/postgres"

# YouTube API Key (optional - for engagement metrics)
# Get from: https://console.cloud.google.com/apis/credentials
# Without this key, YouTube videos will still be fetched via RSS but won't have view/like counts
YOUTUBE_API_KEY="your-youtube-api-key-here"
